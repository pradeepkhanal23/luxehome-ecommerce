module.exports = {
  env: {
    node: true,
  },
  parserOptions: {
    ecmaVersion: 6,
    sourceType: "script", // Treat files as scripts (CommonJS)
  },
  rules: {
    // Add any specific rules or overrides for the functions directory if needed
  },
};
